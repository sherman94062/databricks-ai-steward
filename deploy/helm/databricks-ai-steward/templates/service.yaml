apiVersion: v1
kind: Service
metadata:
  name: {{ include "databricks-ai-steward.fullname" . }}
  labels:
    {{- include "databricks-ai-steward.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "databricks-ai-steward.selectorLabels" . | nindent 4 }}
